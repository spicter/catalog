netdata:
  image: titpetric/netdata:latest
  labels:
    io.rancher.scheduler.global: 'true'
  cap_add:
  - SYS_PTRACE
  volumes:
  - /proc:/host/proc:ro
  - /sys:/host/sys:ro
  ports:
  - 19999:19999/tcp
  environment:
    NETDATA_PORT: "${NETDATA_PORT}"
